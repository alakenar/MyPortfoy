<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myportfoy">
<uses-permission android:name="android.permission.INTERNET"> </uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Myportfoy">
        <activity
            android:name=".MainActivity">

        <intent-filter>
              <action android:name="android.intent.action.MAIN" />

              <category android:name="android.intent.category.LAUNCHER" />
          </intent-filter>

      </activity>

          <activity android:name=".LoginActivity">   </activity>
        <activity android:name=".HomepageActivity">   </activity>
        <activity android:name=".KisiselNotlar">   </activity>
        <activity android:name=".MusteriListesi">   </activity>
        <activity android:name=".TarihselListe">   </activity>
        <activity android:name=".YeniKayit">   </activity>





    </application>

</manifest>